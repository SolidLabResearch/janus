{
  "name": "janus",
  "version": "0.1.0",
  "description": "Janus - A hybrid engine for unified Live and Historical RDF Stream Processing",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint 'src/**/*.{ts,tsx}'",
    "lint:fix": "eslint 'src/**/*.{ts,tsx}' 'tests/**/*.{ts,tsx}' --fix",
    "format": "prettier --write 'src/**/*.{ts,tsx,json}' 'tests/**/*.{ts,tsx,json}' '*.{json,md}'",
    "format:check": "prettier --check 'src/**/*.{ts,tsx,json}' 'tests/**/*.{ts,tsx,json}' '*.{json,md}'",
    "clean": "rm -rf dist coverage",
    "prepare": "husky install || true",
    "prepublishOnly": "npm run build",
    "run:oxigraph": "(command -v docker >/dev/null 2>&1 && (docker ps | grep -q oxigraph-server || docker run -d -p 7878:7878 --name oxigraph-server oxigraph/oxigraph) && sleep 3) || echo 'Docker not available, assuming Oxigraph server is running'; npx ts-node examples/basic-usage.ts --oxigraph",
    "run:jena": "(command -v docker >/dev/null 2>&1 && (docker ps | grep -q jena-server || docker run -d -p 3030:3030 --platform linux/amd64 -v $(pwd)/fuseki-config:/fuseki/configuration -v $(pwd)/fuseki-config/shiro.ini:/fuseki/shiro.ini --name jena-server stain/jena-fuseki) && sleep 3) || echo 'Docker not available, assuming Jena Fuseki server is running'; JENA_DATASET=\"ds\" npx ts-node examples/basic-usage.ts --jena",
    "run:advanced": "(command -v docker >/dev/null 2>&1 && (docker ps | grep -q oxigraph-server || docker run -d -p 7878:7878 --name oxigraph-server oxigraph/oxigraph) && sleep 3) || echo 'Docker not available, assuming Oxigraph server is running'; npx ts-node examples/basic-usage.ts --advanced",
    "run:all": "(command -v docker >/dev/null 2>&1 && ((docker ps | grep -q oxigraph-server || docker run -d -p 7878:7878 --name oxigraph-server oxigraph/oxigraph) && (docker ps | grep -q jena-server || docker run -d -p 3030:3030 --platform linux/amd64 -v $(pwd)/fuseki-config:/fuseki/configuration -v $(pwd)/fuseki-config/shiro.ini:/fuseki/shiro.ini --name jena-server stain/jena-fuseki) && sleep 3)) || echo 'Docker not available, assuming servers are running'; JENA_DATASET=\"ds\" npx ts-node examples/basic-usage.ts"
  },
  "keywords": [
    "rdf",
    "oxigraph",
    "kolibrie",
    "apache-jena",
    "sparql",
    "typescript",
    "semantic-web",
    "stream-processing",
    "rsp-ql"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0",
    "kafkajs": "^2.2.4",
    "mqtt": "^5.14.1",
    "n3": "^1.17.2",
    "rdf-parse": "^1.8.0",
    "rdf-store-stream": "^1.3.0",
    "rsp-js": "^1.3.5",
    "sparql-http-client": "^2.4.1",
    "streamify-string": "^1.0.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/mqtt": "^0.0.34",
    "@types/node": "^20.10.6",
    "@types/streamify-string": "^1.0.4",
    "@typescript-eslint/eslint-plugin": "^6.17.0",
    "@typescript-eslint/parser": "^6.17.0",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-import-resolver-typescript": "^3.10.1",
    "eslint-plugin-import": "^2.29.1",
    "eslint-plugin-jest": "^27.6.1",
    "husky": "^8.0.3",
    "jest": "^29.7.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.1.1",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md}": [
      "prettier --write"
    ]
  }
}
