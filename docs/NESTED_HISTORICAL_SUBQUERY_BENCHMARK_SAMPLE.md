# Nested Historical Subquery Benchmark Sample

This file is a sample-report placeholder, not a benchmark result.

For a reproducible run, record:

```text
binary and command:
Git revision:
query fixture:
historical data size and source:
warm-ups and measured runs:
raw-output path:
result-equivalence evidence:
reported statistic and unit:
```

Run `cargo run --release --bin historical_materialized_subquery_benchmark -- --help`
to inspect the checked-out command surface. See
[Nested Historical Subqueries](./NESTED_HISTORICAL_SUBQUERIES.md) for supported
query shapes.
